# TOPIC
- Binary search we can tell because:
 - Requires logn runtime (binary search?)
 - Fits the narative because array is somewhat sorted 

- Essentially two sorted arrays
- Answer is the left most element between those two sorted arrays


